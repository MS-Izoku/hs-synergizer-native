import React from "react";
import { View } from "react-native";

const headerStyleSheet = {
  backgroundColor: "red"
};

const AppHeader = () => {
  return <View style={headerStyleSheet}>App Header</View>;
};

export default AppHeader;
