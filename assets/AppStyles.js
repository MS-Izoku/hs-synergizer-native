import {StyleSheet} from 'react-native'

// styling containers that have other elements
export const containerStyle = StyleSheet.create({})

// styling paragraph-like elements
export const paragraphs = StyleSheet.create({})


// style for individual cards
export const cardStyle = StyleSheet.create({
    container: {},
    cardName: {},
    cost: {},
    baseStat: {},
    cardImg: {},
    cardText: {}
})

export const footerStyle = StyleSheet.create({
    mainAppFooter:{
        background: "blue",
        text: "white"
    }
})

export const headerStyle = StyleSheet.create({
    mainAppHeader:{
        background: "red",
        color: "white"
    }
})

// styling the app navigation elements
export const navStyle = StyleSheet.create({
    button:{},
})